import styled from '@emotion/styled';

const Main = styled.div`
  flex: 1;
  height: calc(100vh - 64px);
  margin-left: 40px;
  overflow: auto;
  background-color: gray;
`;

export default {
  Main,
};
